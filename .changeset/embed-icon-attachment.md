---
"@vex-chat/libvex": minor
---

`MessageEmbed` gains an optional `iconAttachment` field (`EncryptedFileAttachmentReference`) so clients can attach an encrypted file as the embed icon instead of a plain icon URL string.
